# API Configuration
VITE_API_URL=http://localhost:4000/api

# App Configuration
VITE_APP_NAME=Task Manager
VITE_APP_VERSION=1.0.0

# Environment
VITE_ENV=development

# Push Notifications
# VAPID public key for push notifications (obtener del backend con php artisan vapid:generate)
VITE_VAPID_PUBLIC_KEY=
